# Lesson 12 — Deploying Production Applications

**Status:** Not started

Adds a `Dockerfile` per app plus a GitHub Actions workflow that deploys only the changed app(s) using `turbo run build --filter=...`.
